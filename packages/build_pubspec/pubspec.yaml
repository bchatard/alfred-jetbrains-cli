name: build_pubspec
description: Extract pubspec data
version: 1.3.0

environment:
  sdk: ^3.8.0

dependencies:
  build: ^2.3.1
  pubspec_parse: ^1.2.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.21.0
