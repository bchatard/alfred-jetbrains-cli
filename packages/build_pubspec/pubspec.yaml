name: build_pubspec
description: Extract pubspec data
version: 1.2.1

environment:
  sdk: ^3.7.0

dependencies:
  build: ^2.3.1
  pubspec_parse: ^1.2.2

dev_dependencies:
  lints: ^5.1.1
  test: ^1.21.0
